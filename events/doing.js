const Discord = require("discord.js");
setInterval(myMethod, 5000);
//the current interval is 5 seconds. 1000 ms is 1s.
//do not use.
function myMethod( )
{
client.user.setActivity("on Servers! || !help");
}